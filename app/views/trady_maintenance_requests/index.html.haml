-edit_trady = edit_trady_path(current_user.trady.id)
-user_name = current_user.trady.name || ''
- images = profile_and_company_avatar

= react_component 'ListMaintenanceRequest', {:link => "/trady_maintenance_requests", :current_user_trady => @current_user.trady, :maintenance_requests => @maintenance_requests_json, :quote_request => @quote_request, :awaiting_quote_approvals => @awaiting_quote_approvals, :appointments_required =>  @appointments_required, :awaiting_appointment_confirmation => @awaiting_appointment_confirmation, :alternate_appointment_requested => @alternate_appointment_requested, :job_booked => @job_booked, :awaiting_payment => @awaiting_payment, :job_complete => @job_complete, :declined_quotes => @declined_quotes, :cancelled_work_orders=>@cancelled_work_order_count,:authenticity_token => form_authenticity_token, :edit_trady => edit_trady, :user_name => user_name, :images => images, logout_path: logout_path, :current_role => @current_user.current_role, :jfmo_participant => current_user.trady.jfmo_participant}
