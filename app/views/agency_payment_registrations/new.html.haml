:javascript
  stripe = Stripe('#{Rails.configuration.stripe[:publishable_key]}');

%div.well.container
  =react_component('AgencyRegistrationForm', {:authenticity_token => form_authenticity_token, :agency_id => @agency_id, :step => "payment"})
