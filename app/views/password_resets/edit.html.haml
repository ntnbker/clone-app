%h1 Choose a new password
= bootstrap_form_for @user,url: password_reset_path(@token), :html => {:method => :put} do |f| 
  = f.label :email 
  = @user.email 
  = f.label :password 
  = f.password_field :password 
  = f.label :password_confirmation 
  = f.password_field :password_confirmation
  = f.submit 
