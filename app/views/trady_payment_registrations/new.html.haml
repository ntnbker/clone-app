:javascript
  stripe = Stripe('#{Rails.configuration.stripe[:publishable_key]}');

%div.container
  =react_component('TradyPayment', {:authenticity_token => form_authenticity_token, :trady_company_id => @trady_company_id, :trady_id => @trady_id, :maintenance_request_id => @maintenance_request_id})
