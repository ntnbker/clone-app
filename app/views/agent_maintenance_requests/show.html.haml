-edit_agent = edit_agent_path(current_user.agent.id)
-user_name = current_user.agent.name || ''
- images = profile_and_company_avatar

= react_component 'MaintenanceRequest', {:landlord => @landlord, :quotes => @quotes, :property => @maintenance_request.property, :invoices => @maintenance_request.delivered_invoices, :gallery => @gallery, :authenticity_token => form_authenticity_token, :maintenance_request => @maintenance_request, :tenants_conversation => @tenants_conversation, :landlords_conversation => @landlords_conversation, :current_user => @current_user, :agency => @agency, :current_user_show_quote_message => @current_user.agent, :tradies => @all_tradies, :invoices => @invoices, :invoice_pdf_files => @invoice_pdf_files, :logs => @logs, :tradies_with_quote_requests => @quote_request_trady_list, :work_order_appointments => @work_order_appointments, :landlord_appointments => @landlord_appointments, :quote_appointments => @quote_appointments, :all_agents => @all_agents, :all_agency_admins => @all_agency_admins, :current_user_role => @current_user.current_role, :tenants => @tenants, :assigned_trady => @assigned_trady, :trady_conversation => @trady_agent_conversation, :status => @status, :services => @services, :instruction => @instruction, :pdf_urls => @invoice_pdf_urls, :deferred_count => @deferred_count, :archived_count => @archived_count, :hired_trady => @hired_trady, pdf_files: @pdf_files, :quote_requests => @quote_requests, :agent => @agent, :agency_admin => @agency_admin, :edit_agent => edit_agent, :user_name => user_name, :images => images, logout_path: logout_path}
