!!! 5
%html(lang="en-US")
  
  %body
    %p 
      Hello, "#{@trady.name.capitalize}" 
      We would love to ask for a quote for the following job. The information on the complaint can be found below

    %h3
      =@maintenance_request.maintenance_heading
    %p
      =@maintenance_request.maintenance_description

    %p If you can send us a Quote on how much it would cost to get this fixed that would be great. To submit the Quote click on the link below. 

    =link_to "View Maintenance Request", trady_maintenance_request_url(id:@maintenance_request.id,user_id:@trady.user.id)


    / -if @trady.trady_company.nil?
    /   %button.button-primary.green.option-button
    /     =link_to "Create Quote(New company)", new_trady_company_url(maintenance_request_id:@maintenance_request.id,trady_id:@trady.id, quote_type:"system_quote", system_plan:"Quote", user_id:@trady.user.id)
    / -else 
    /   %button.button-primary.green.option-button
    /     =link_to "Create Quote(Edit Company)", edit_trady_company_url(@trady.trady_company,maintenance_request_id:@maintenance_request.id,trady_id:@trady.id, quote_type:"system_quote", system_plan:"Quote", user_id:@trady.user.id)