-user_name = current_user.landlord.name || ''
- images = profile_and_company_avatar

= react_component 'ListMaintenanceRequest', {:link => "/landlord_maintenance_requests", :current_user_landlord => @current_user.landlord, :maintenance_requests => @maintenance_requests_json, :authenticity_token => form_authenticity_token,:agent => @agent, :images => images, :user_name => user_name, logout_path: logout_path, :current_role => @current_user.current_role}
