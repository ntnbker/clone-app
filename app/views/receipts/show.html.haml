-edit_trady = edit_trady_path(current_user.trady.id)
-user_name = current_user.trady.name || ''
- images = profile_and_company_avatar

=react_component('Receipt', {:authenticity_token => form_authenticity_token, :edit_trady => edit_trady, :user_name => user_name, :images => images, logout_path: logout_path, :current_role => @current_user.current_role, :receipt => @receipt, :link => '/trady_maintenance_requests'})